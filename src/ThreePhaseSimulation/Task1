(W) a) Why does an arrival event need to generate a new arrival event? b) Which event should be generated by the start of the service?
    a) Arrival events often trigger the generation of new events to maintain continuous activity in event-driven systems.
    b) The event generated at the start of a service varies by system; in queue systems, it might be an "initiate service" event, marking the start of processing.